<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Centered Rounded Divs using Bootstrap</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheet/history.css">
</head>
<body>
    <%if(filteredData.length==0){%>
    <div class="container">
        <h1>No Jobs History Found</h1>
    </div>
    <%}else{%>
        <div class="container flex-column">
        <%filteredData.forEach((job)=>{%>
                <div class="card rounded-pill col-12 my-4 px-4 py-2 border-4 border-dark">
                    <div class="card-body text-center">
                        <p class="card-text"><%=job.createdAt%></p>
                        <p class="card-title fs-1">job-title:<%=job.profession%></p>
                        <p class="card-text fs-3">salary:<%=job.salary%></p>
                        <p class="card-text fs-3">no_workers:<%=job.no_workers%></p>
                    </div>
                </div>
        <%})%>
        </div>
<%}%>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
